import React from 'react';
import ErrorPageContainer from '~shared/components/containers/error/error.page.container';

const ErrorPage: React.FC = () => {
	return <ErrorPageContainer />;
};

export default ErrorPage;
