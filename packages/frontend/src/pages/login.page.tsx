import React from 'react';
import LoginContainer from '~shared/components/containers/login/login.page.container';

const LoginPage: React.FC = () => {
	return <LoginContainer />;
};

export default LoginPage;
