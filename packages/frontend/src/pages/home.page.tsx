import React from 'react';
import HomePageContainer from '~shared/components/containers/home/home.page.container';

const HomePage: React.FC = () => {
	return <HomePageContainer />;
};

export default HomePage;
