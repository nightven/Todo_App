import React from 'react';
import TodoContainer from '~shared/components/containers/todos/todos.container';

const TodoDetailPage: React.FC = () => {
	return <TodoContainer />;
};

export default TodoDetailPage;
