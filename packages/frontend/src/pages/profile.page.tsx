import React from 'react';
import ProfilePageContainer from '~shared/components/containers/profile/profile.page.container';

const ProfilePage: React.FC = () => {
	return <ProfilePageContainer />;
};

export default ProfilePage;
