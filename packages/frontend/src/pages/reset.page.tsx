import React from 'react';
import ResetPageContainer from '~shared/components/containers/reset/reset.password.page.container';

const ResetPage: React.FC = () => {
	return <ResetPageContainer />;
};

export default ResetPage;
